<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppTempoAgora.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <!-- Ajuste: corrigido x:Name para o Entry ser acessível no code-behind -->
            <Entry x:Name="txt_cidade"/>
            <Button Text="Buscar" Clicked="Button_Clicked"/>
            <!-- Ajuste: removido ponto no nome para virar identificador válido -->
            <Label x:Name="lbl_res"/>
            





        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
